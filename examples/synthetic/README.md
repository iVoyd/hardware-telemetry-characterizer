# Synthetic Examples

Every file in this directory is synthetic and uses generic identifiers. The
values are illustrative only; they are not physical measurements, calibrated
results, acceptance limits, or production evidence.
